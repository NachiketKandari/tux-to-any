/* BAT_ADV_NO_SQL — a main() with pure logic and zero EXEC SQL: nothing to
 * convert; must degrade visibly (no constants, no DAL, retention N/A). */
#include <stdio.h>
#include <string.h>

void main(int argc,char *argv[])
{
    int i;
    char buf[64];

    for(i = 0; i < 10; i++)
    {
        sprintf(buf,"iteration %d",i);
    }
    printf("%s\n",buf);
}

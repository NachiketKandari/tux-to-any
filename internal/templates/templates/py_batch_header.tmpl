"""{{.Module}} — generated by tuxgo batchpy from {{.SourcePath}}.

Source batch: {{.ServiceName}} (entry {{.Entry}}).
Shape: {{.Shape}} · DML loop: {{.DMLLoop}} · transactions: {{.RouterClass}} context manager owns commit/rollback.
Template set: tuxgo py v1.
"""
import logging
from typing import Any, Dict, Iterator, List, Optional, Tuple

import oracledb

from {{.WrapperImport}} import {{.RouterClass}}, DbMode

logger = logging.getLogger("{{.LoggerName}}")

CHUNK_SIZE = {{.ChunkSize}}
